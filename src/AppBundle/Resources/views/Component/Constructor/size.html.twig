<div class="col-md-12 az-picture-size-div container-fluid">
    <div class="row az-picture-page-sidebar-size">
        <div class="col-md-4 text-left az-picture-page-sidebar-header-block az-picture-page-sidebar-size-div-left">
            <label data-for="picture-size">Размер: </label>
        </div>
        <div class="col-md-8 text-right az-picture-page-sidebar-div-right">
            <button class="btn az-picture-page-sidebar-size-btn az-picture-page-hint">Подробнее ?</button>
        </div>
    </div>
    <div class="row az-picture-page-sidebar-size-div-body az-picture-page-sidebar-size-div-select">
        <select class="btn az-picture-page-sidebar-size-select" name="picture-size" id="az-picture-page-constructor-size-select" title="">
            <option value="color">Цветопроба</option>
            <option value="own_size">Свой размер</option>
            {% if pictureSize %}
                {% set size_default = 1 %}
                {% for size in pictureSize %}
                    <option value="{{ size.width }}x{{ size.height }}" {% if size_default == 1 %}selected{% endif %}>{{ size.width }}x{{ size.height }}</option>
                    {% set size_default = size_default + 1 %}
                {% endfor %}
            {% endif %}
        </select>
    </div>
</div>